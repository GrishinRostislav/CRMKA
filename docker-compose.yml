version: '3.8'

services:
  # Backend Service
  backend:
    build: ./server
    container_name: crmka-backend
    volumes:
      - crmka_db_data:/app/data
    environment:
      - DB_PATH=/app/data/crmka.db
    restart: always

  # Frontend Service (Nginx)
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"

    depends_on:
      - backend
    restart: always

# Persistent storage for SQLite
volumes:
  crmka_db_data:
